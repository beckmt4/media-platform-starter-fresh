version: 1

server:
  host: "0.0.0.0"
  port: 8000
  log_level: info

# Storage backend — "memory" is the only supported value in the skeleton.
# Replace with "sqlite" or "postgres" for persistence.
storage:
  backend: memory
