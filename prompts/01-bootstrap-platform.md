# Bootstrap Prompt

Read `CLAUDE.md`, `docs/000-architecture-overview.md`, `docs/010-repo-strategy.md`, and all files under `config/`.

Then do the following:

1. summarize the current repo contract
2. identify missing pieces needed to bootstrap the platform
3. propose the smallest first implementation slice
4. list files to create or update
5. describe test coverage needed
6. do not move runtime media workloads into GitHub-hosted CI
7. keep all operational AI local
